success = import("general", "console", "math");

if (success) {
    console.println("Successfully imported libraries!");
    {
        name = "Elias";
        console.println("Hello ", name, "!");
        console.println(math.pi());
    }
} else {
    console.println("Import of a library failed");
}
